<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.april.project_uas">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_UAS"
        android:usesCleartextTraffic="true">
        <activity android:name=".Developer"></activity>
        <activity android:name=".LupaPassword" />
        <activity android:name=".ProfilHmsi" />
        <activity android:name=".Login" />
        <activity android:name=".MenuPengurus" />
        <activity android:name=".MenuAspirasi" />
        <activity android:name=".MenuGaleri" />
        <activity android:name=".MenuEvent" />
        <activity android:name=".registrasi" />
        <activity android:name=".Dashboard" />
        <activity android:name=".MainActivity"><intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        </activity>
    </application>

</manifest>